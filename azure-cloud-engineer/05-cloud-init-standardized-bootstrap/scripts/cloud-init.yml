#cloud-config
package_update: true
packages:
  - nginx
  - curl

users:
  - name: victor
    groups: sudo
    shell: /bin/bash
    sudo: "ALL=(ALL) NOPASSWD:ALL"

write_files:
  - path: /var/www/html/index.html
    permissions: "0644"
    content: |
      <h1>Azure cloud-init lab</h1>
      <p> This VM was configured automatically at boot. </p>

      
      <p> Created by Victor. </p>

runcmd:
  - systemctl enable nginx
  - systemctl restart nginx
